# Note - Can easily auto create a docker file with the Docker VS Code Official Extension
# Open Command Pallette (CTRL + SHIFT + P) -> Add DockerFile to workspace -> Node.js -> package.json -> Your port.

# THIS FILE HAS BEEN AUTO GENERATED BY THE DOCKER EXTENSION "Add DockerFile to workspace" COMMAND.

version: "3.4"

services:
  dockernodejsexpressproject:
    image: dockernodejsexpressproject
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 5000:5000


# ------------------------------------

# IMPORTANT - TO BUILD ALL THE IMAGES AND RUN CONTAINERS - USE FOLLOWING COMMAND IN BASH TERMINAL 
#   - docker compose up --build -d

# TO STOP ALL CONTAINERS AND REMOVE ALL IMAGES CREATED FROM THIS DOCKER COMPOSE FILE - RUN COMMAND 
#   - docker compose down