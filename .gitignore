#/: This indicates that the rule applies to the root of the repository. Without the leading /, the rule would apply to all subdirectories as well.
# *.txt.bak: This pattern matches any file that ends with the .txt.bak extension.
/*.txt.bak

# Terraform plugin directory (auto-generated by `terraform init`)
.terraform/

# Terraform state files (can contain sensitive data)
*.tfstate
*.tfstate.backup

# Terraform plan output
*.tfplan

# Terraform crash logs
crash.log

# DO NOT ignore the .terraform.lock.hcl file!
# This lock file ensures consistent provider versions across environments.
# It SHOULD be committed to version control.
# https://developer.hashicorp.com/terraform/cli/lifecycle/locks
# !.terraform.lock.hcl
